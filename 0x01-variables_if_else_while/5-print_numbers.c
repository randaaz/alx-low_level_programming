#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: using while loop and if function
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int ran = 0;

	while (ran <= 9)
	{
		printf("%d", ran);
		ran++;
	}
	printf("\n");
	return (0);
}
